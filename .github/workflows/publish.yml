# Builds and publishes the OneCLI self-host images — one per
# docker/<service>.Dockerfile — on every v* tag: multi-arch (amd64 + arm64)
# manifests at ghcr.io/whybutter/onecli-<service>, tagged <version>,
# <major>.<minor>, and (non-prerelease tags only) latest — exactly the names
# docker/docker-compose.yml pulls, so `docker compose up` runs from pre-built
# images with no source build.
#
# Maintainer-owned, kept in lockstep with release.yml: the repository guard on
# every job makes this workflow a no-op anywhere but whybutter/onecli (forks and
# mirrored copies included). Never delete this file — synced copies propagate
# a deletion.
name: Publish

on:
  push:
    tags:
      - "v*"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    # Display name only — without it every run shows 14 rows of
    # "build (<service>, <arch>)"; this renders "gateway (amd64)" instead.
    name: ${{ matrix.service }} (${{ matrix.arch }})
    if: github.repository == 'whybutter/onecli'
    strategy:
      matrix:
        # We only build the four images our compose actually runs: web, api and
        # gateway are our own code, migrations is the one-shot schema runner the
        # api gates on. `runner`/`agent`/`channel-adapter`/`ssh-terminator` stay
        # pointed at upstream's published images (see docker-compose.yml) — we
        # don't build the hosted-agent stack. Paths derive by convention
        # (docker/<service>.Dockerfile).
        service: [web, api, gateway, migrations]
        arch: [amd64, arm64]
        include:
          - arch: amd64
            runner: ubuntu-latest
            platform: linux/amd64
          - arch: arm64
            runner: ubuntu-24.04-arm
            platform: linux/arm64
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.service }}

      - name: Resolve app version
        id: appversion
        run: echo "value=$(jq -r .version package.json)" >> "$GITHUB_OUTPUT"

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/${{ matrix.service }}.Dockerfile
          platforms: ${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_VERSION=${{ steps.appversion.outputs.value }}
          outputs: type=image,name=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.service }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=${{ matrix.service }}-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}-${{ matrix.arch }}

      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v7
        with:
          name: digest-${{ matrix.service }}-${{ matrix.arch }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    name: ${{ matrix.service }} (manifest)
    if: github.repository == 'whybutter/onecli'
    runs-on: ubuntu-latest
    needs: build
    strategy:
      matrix:
        # We only build the four images our compose actually runs: web, api and
        # gateway are our own code, migrations is the one-shot schema runner the
        # api gates on. `runner`/`agent`/`channel-adapter`/`ssh-terminator` stay
        # pointed at upstream's published images (see docker-compose.yml) — we
        # don't build the hosted-agent stack. Paths derive by convention
        # (docker/<service>.Dockerfile).
        service: [web, api, gateway, migrations]
    permissions:
      contents: read
      packages: write

    steps:
      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: /tmp/digests
          pattern: digest-${{ matrix.service }}-*
          merge-multiple: true

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.service }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ !contains(github.ref_name, '-') }}

      - name: Create multi-arch manifest and push
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf '${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.service }}@sha256:%s ' *)

      - name: Inspect image
        run: |
          docker buildx imagetools inspect ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.service }}:${{ steps.meta.outputs.version }}
